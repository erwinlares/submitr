% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-helpers.R
\name{.get_manifest}
\alias{.get_manifest}
\title{Retrieve the current job manifest}
\usage{
.get_manifest(path = ".")
}
\arguments{
\item{path}{A character string. Directory to look for
\code{htc-manifest.yaml} in. Defaults to \code{"."} (current working
directory).}
}
\value{
A named list or \code{NULL}.
}
\description{
Internal helper that reads the accumulated job manifest from
\code{htc-manifest.yaml} in \code{path}. Returns \code{NULL} if no manifest file
exists yet.
}
\details{
YAML represents a sequence (e.g. a character vector recorded via
\code{.update_manifest()}) as a list on read-back. Each top-level element
that is a list of length-1 atomic values is simplified back into an
ordinary vector here, so callers see the same shape they originally
passed to \code{.update_manifest()}.
}
\keyword{internal}
