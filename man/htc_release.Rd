% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-job-control.R
\name{htc_release}
\alias{htc_release}
\title{Release held HTCondor jobs}
\usage{
htc_release(
  cluster_id = NULL,
  config = NULL,
  dry_run = FALSE,
  verbose = FALSE,
  path = "."
)
}
\arguments{
\item{cluster_id}{An integer, character string, or \code{NULL}. The cluster ID
to release, e.g. \code{6302860}. When \code{NULL} (the default), resolves to the
cluster ID recorded in the job manifest by the most recent
\code{\link[=htc_submit]{htc_submit()}} call. Unlike \code{\link[=htc_status]{htc_status()}}, \code{htc_release()} never falls
back to "all of my jobs": a bare \code{condor_release} with no arguments
releases every held job you have on the submit node, not just the ones
from this project, so the function errors instead of guessing when no
cluster ID is available anywhere.}

\item{config}{A named list as returned by \code{\link[=htc_config]{htc_config()}}. Must contain
\code{username} and \code{server}. If \code{NULL} (the default), uses the session
config set by \code{\link[=htc_start]{htc_start()}}. If no session config is set, the function
errors with instructions.}

\item{dry_run}{Logical. If \code{TRUE}, prints the SSH command that would be
executed without running it. Defaults to \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages. Defaults to
\code{FALSE}.}

\item{path}{A character string. Directory holding the job manifest
(\code{htc-manifest.yaml}), consulted only when \code{cluster_id} is \code{NULL}.
Defaults to \code{"."}, matching the default used elsewhere in the family.}
}
\value{
The \code{condor_release} output as a character vector, returned
invisibly.
}
\description{
\code{htc_release()} connects to an HTC submit node via SSH and runs
\code{condor_release} to un-pause jobs that HTCondor has held (S-G2) --
typically after fixing whatever caused the hold, such as raising a
resource request reported by \code{\link[=htc_status]{htc_status()}}'s hold-reason output.
}
\section{Workflow}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{cfg <- htc_config()
htc_status(cluster_id = 6302860, config = cfg)
# ... shows job 6302860.0 held; hold reason printed below the table ...
# ... fix the resource request that caused the hold, then:
htc_release(cluster_id = 6302860, config = cfg)
}\if{html}{\out{</div>}}
}

\examples{
\donttest{
# Preview the SSH command without connecting to CHTC
cfg <- list(username = "netid", server = "ap2002.chtc.wisc.edu")
htc_release(cluster_id = 6302860, config = cfg, dry_run = TRUE)
}

\dontrun{
cfg <- htc_config()
htc_release(cluster_id = 6302860, config = cfg)
}
}
\seealso{
\code{\link[=htc_cancel]{htc_cancel()}} to remove a job instead of releasing it, and
\code{\link[=htc_status]{htc_status()}} to see hold reasons.
}
