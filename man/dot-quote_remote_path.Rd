% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-helpers.R
\name{.quote_remote_path}
\alias{.quote_remote_path}
\title{Quote a remote path while leaving a leading tilde expandable}
\usage{
.quote_remote_path(path)
}
\arguments{
\item{path}{A character string. A remote directory, possibly beginning
with \code{~} or \code{~user}.}
}
\value{
A character string safe to interpolate into a remote command.
}
\description{
Internal helper. Remote paths reach the submit node through two shells:
\code{system2()} runs the \code{ssh} invocation through a local shell, and \code{sshd}
runs the command string through a shell on the far end. A leading \code{~} has
to survive the first unexpanded and then be expanded by the second, so it
cannot simply be handed to \code{shQuote()}: inside single quotes a tilde is a
literal character, and \verb{cd '~/'} fails.
}
\details{
The fix is to hold the tilde outside the quotes and quote only what
follows. A shell concatenates adjacent quoted and unquoted fragments into
a single word, so \verb{~/'my data/'} reaches \code{cd} as one argument, with the
tilde expanded and the space preserved.
}
\keyword{internal}
