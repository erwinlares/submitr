% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-ssh-setup.R
\name{htc_ssh_setup}
\alias{htc_ssh_setup}
\title{Configure SSH connection reuse (ControlMaster) for CHTC}
\usage{
htc_ssh_setup(
  ssh_config_path = "~/.ssh/config",
  connections_dir = "~/.ssh/connections",
  host_pattern = "*.chtc.wisc.edu",
  dry_run = FALSE
)
}
\arguments{
\item{ssh_config_path}{A character string. Path to the SSH client config
file to append to. Defaults to \code{"~/.ssh/config"}.}

\item{connections_dir}{A character string. Path to the directory
\code{ControlPath} will use for its control sockets. Defaults to
\code{"~/.ssh/connections"}. Written into the config file exactly as given
(so a leading \code{~} is expanded by \code{ssh} itself at connection time, not by
R), and created on disk if it does not already exist.}

\item{host_pattern}{A character string. The \code{Host} pattern the block
applies to. Defaults to \code{"*.chtc.wisc.edu"}, matching every CHTC submit
node. Narrow this if you only want reuse for one specific host.}

\item{dry_run}{Logical. If \code{TRUE}, prints what would be added and created
without writing anything. Defaults to \code{FALSE}.}
}
\value{
Called for its side effects. Returns \code{invisible(NULL)}.
}
\description{
\code{htc_ssh_setup()} writes the ControlMaster block described in
\code{\link[=htc_config]{htc_config()}}'s own setup guidance to \verb{~/.ssh/config} and creates the
connections directory it references, so that reuse can be set up without
leaving R (S-G3). Every function in this package that opens an SSH
connection -- \code{\link[=htc_upload]{htc_upload()}}, \code{\link[=htc_submit]{htc_submit()}}, \code{\link[=htc_status]{htc_status()}},
\code{\link[=htc_download]{htc_download()}}, \code{\link[=htc_cancel]{htc_cancel()}}, and \code{\link[=htc_release]{htc_release()}} -- benefits, since
each currently opens a fresh connection per call and triggers a separate
Duo MFA prompt without this.
}
\section{What this does not do}{

If a \code{Host} block matching \code{host_pattern} already exists in
\code{ssh_config_path}, \code{htc_ssh_setup()} leaves the file untouched and says
so -- it never edits or replaces an existing block, since the existing
one may have been customized deliberately. Remove or edit it by hand
first if you want \code{htc_ssh_setup()} to write a fresh one.
}

\section{Windows}{

\code{ControlPath} relies on Unix domain sockets, which native Windows OpenSSH
(as shipped with Windows 10/11) has historically supported inconsistently
across versions. \code{htc_ssh_setup()} still writes the block on Windows --
it may simply work, particularly on a current OpenSSH release -- but if
\code{ssh} subsequently errors mentioning \code{ControlPath} or a socket, connection
reuse is not supported on this system: remove the added block and expect
a Duo MFA prompt on every call instead. OpenSSH bundled with WSL or with
Git Bash is more likely to support this than the native Windows client.
}

\examples{
\donttest{
# Preview without touching any files
htc_ssh_setup(dry_run = TRUE)
}

\dontrun{
htc_ssh_setup()

# Apply only to one specific submit node
htc_ssh_setup(host_pattern = "ap2002.chtc.wisc.edu")
}
}
\seealso{
\code{\link[=htc_config]{htc_config()}}, whose own first-run message describes this same
block for manual setup.
}
