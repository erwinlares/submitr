% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-collect.R
\name{.resolve_collected_tarballs}
\alias{.resolve_collected_tarballs}
\title{Resolve the local tarballs htc_download() saved, keyed by group id}
\usage{
.resolve_collected_tarballs(manifest, local_path)
}
\arguments{
\item{manifest}{A named list from \code{.get_manifest()}, or \code{NULL}.}

\item{local_path}{A character string. Directory the tarballs were
downloaded into.}
}
\value{
A tibble with columns \code{group_id} and \code{tarball} (a local path),
possibly with zero rows.
}
\description{
Internal helper used by \code{\link[=htc_collect]{htc_collect()}} to rebuild, from the job
manifest, the same tarball names \code{\link[=htc_download]{htc_download()}} would have fetched into
\code{local_path} -- reusing \code{\link[=.tarball_name]{.tarball_name()}} so the naming stays in the one
place the rest of the family already shares it (see that function's own
docs for why three separate resolvers of this name would drift).
}
\keyword{internal}
