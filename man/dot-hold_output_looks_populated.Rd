% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-status.R
\name{.hold_output_looks_populated}
\alias{.hold_output_looks_populated}
\title{Detect whether a condor_q -hold report actually shows a held job}
\usage{
.hold_output_looks_populated(output, cluster_id)
}
\arguments{
\item{output}{A character vector. The lines returned by
\code{condor_q -hold}.}

\item{cluster_id}{A character string or \code{NULL}.}
}
\value{
A logical scalar.
}
\description{
Internal helper used by \code{htc_status()} to decide whether the follow-up
\code{condor_q -hold} query (S-G2) found anything worth printing. \code{condor_q}
prints a schedd header (an address, a port, a timestamp) whether or not
any job is actually held, so an empty-but-for-the-header report must not
be mistaken for one describing a held job.
}
\details{
The check looks for something shaped like a job ID (\code{ClusterId.ProcId},
e.g. \code{6302860.0}), anchored to \code{cluster_id} when one is known. This is
the same job-ID-pattern approach \code{.jobs_in_queue()} falls back to, and
for the same reason: it does not depend on \code{condor_q}'s column layout,
which is not a documented contract this package can rely on.
}
\keyword{internal}
