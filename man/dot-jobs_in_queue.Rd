% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-status.R
\name{.jobs_in_queue}
\alias{.jobs_in_queue}
\title{Count the jobs a condor_q report still shows for a cluster}
\usage{
.jobs_in_queue(output, cluster_id)
}
\arguments{
\item{output}{A character vector. The lines returned by \code{condor_q}.}

\item{cluster_id}{A character string. The cluster being watched.}
}
\value{
An integer count of jobs still in the queue.
}
\description{
Internal helper used by \code{htc_status(watch = TRUE)} to decide when to stop
polling.
}
\details{
The obvious test, searching the report for the cluster ID as a substring,
is not safe. \code{condor_q} always prints a schedd header carrying an address,
a port and a timestamp, and closes with summary lines such as
\verb{Total for all users: 3402 jobs; ...}. Digits from any of those can
coincide with the cluster ID, and because the test drives the loop's exit,
a false match does not produce a wrong answer -- it produces a watch loop
that never returns.

The report's own \verb{Total for query:} line is a direct answer instead. The
remote command is \verb{condor_q <cluster_id>}, so the query is this cluster,
and the count is the number of its jobs still in the queue. If that line
is missing, the fallback matches the cluster ID anchored to the \code{.} that
separates it from the process number, which restricts it to the \code{JOB_IDS}
column (\code{6302860.0}) rather than the report at large.
}
\keyword{internal}
