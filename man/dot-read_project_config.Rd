% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-helpers.R
\name{.read_project_config}
\alias{.read_project_config}
\title{Read a toolero project configuration file}
\usage{
.read_project_config(path)
}
\arguments{
\item{path}{A character string. Path to a \verb{_toolero.yml} file.}
}
\value{
A named list with elements \code{folders} (a character vector, possibly
\code{NULL}) and \code{conventions} (a named list, possibly \code{NULL}, typically with
\code{output_dir}, \code{script_dir}, and \code{split_dir}).
}
\description{
Internal helper behind \code{htc_config()}'s \code{project_config} argument. Parses
a \verb{_toolero.yml} file (the resolved instance \code{toolero::init_project()}
writes to a project's root, not the internal template it renders from)
and returns its \code{folders} and \code{conventions} sections.
}
\details{
Like \code{.get_manifest()}, this normalizes YAML's round trip: a sequence of
scalars (\code{folders}) comes back from \code{yaml::read_yaml()} as a list, and
callers expect an ordinary character vector.
}
\keyword{internal}
