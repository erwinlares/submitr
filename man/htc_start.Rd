% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-start.R
\name{htc_start}
\alias{htc_start}
\title{Start an HTC session}
\usage{
htc_start(...)
}
\arguments{
\item{...}{Arguments passed to \code{\link[=htc_config]{htc_config()}}. Common arguments
include \code{username}, \code{server}, \code{path}, and \code{overwrite}.}
}
\value{
Invisibly returns the config list (same as \code{\link[=htc_config]{htc_config()}}).
}
\description{
\code{htc_start()} calls \code{\link[=htc_config]{htc_config()}} to read or create the connection
configuration, then stores the result as a session-level option so
that subsequent \verb{htc_*()} functions can use it without requiring an
explicit \code{config} argument on every call.
}
\details{
After calling \code{htc_start()}, functions like \code{\link[=htc_upload]{htc_upload()}},
\code{\link[=htc_submit]{htc_submit()}}, \code{\link[=htc_status]{htc_status()}}, and \code{\link[=htc_download]{htc_download()}} will
automatically use the stored configuration when \code{config = NULL}
(the default). You can still pass \code{config} explicitly to any
function to override the session config.

The session config is stored via \code{options(submitr.config = ...)} and
is cleared automatically when the R session ends. To clear it
manually, call \code{options(submitr.config = NULL)}.
}
\examples{
\dontrun{
# Start a session -- all subsequent htc_*() calls use this config
htc_start()

# Now these work without config = cfg
htc_upload(files = c("job.sub", "job.sh"))
htc_submit(submit_file = "job.sub")
htc_status(cluster_id = 6351616)
htc_download(files = "*.tar.gz")

# You can still override for a specific call
other_cfg <- htc_config(path = "other-project/")
htc_upload(files = "job.sub", config = other_cfg)

# Clear the session config manually
options(submitr.config = NULL)
}
}
