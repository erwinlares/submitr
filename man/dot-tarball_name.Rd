% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-helpers.R
\name{.tarball_name}
\alias{.tarball_name}
\title{Build the name of a results tarball}
\usage{
.tarball_name(script_stem = NULL, subset_expr = NULL)
}
\arguments{
\item{script_stem}{A character string or \code{NULL}, from \code{.script_stem()}.}

\item{subset_expr}{A character string or \code{NULL}. However the subset stem
is written in the context being generated.}
}
\value{
A character string. At least one of the two parts must be given.
}
\description{
Internal helper holding the family's tarball naming convention in one
place: \verb{<script stem>[-<subset stem>]-results.tar.gz}, with the subset
half present only in multiple mode.
}
\details{
The subset stem takes a different form depending on who resolves it, which
is why this takes an expression rather than a value. The generated shell
script writes \verb{$\{1\%.*\}}, resolved on the execute node. The submit file
writes \verb{$Fn(file)}, resolved by \code{condor_submit}. \code{\link[=htc_download]{htc_download()}} passes a
literal stem it has already computed in R. All three have to agree on the
same name for a job's results to survive the trip home, and sharing this
function is what makes that structural rather than a matter of three
places being edited together.
}
\keyword{internal}
