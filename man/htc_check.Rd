% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-check.R
\name{htc_check}
\alias{htc_check}
\title{Preflight check for a submitr job before upload or submission}
\usage{
htc_check(
  container_image = NULL,
  input_files = NULL,
  data_files = NULL,
  resources = NULL,
  verbose = TRUE,
  path = "."
)
}
\arguments{
\item{container_image}{A character string or \code{NULL}. The container image
reference to check, e.g. \code{"registry.doit.wisc.edu/netid/myimage:1.0.0"}.
When \code{NULL} (the default), resolves to the \code{container_image} recorded
in the job manifest by \code{\link[=htc_gen_submit]{htc_gen_submit()}}. When still \code{NULL}, image
checks are skipped.}

\item{input_files}{A character vector or \code{NULL}. Local paths that are
expected to exist before upload -- normally the same value passed to
\code{\link[=htc_gen_submit]{htc_gen_submit()}}. When \code{NULL} (the default), resolves to
\code{input_files} recorded in the manifest.}

\item{data_files}{A character vector or \code{NULL}. Local paths to data files
expected to exist before a container image bakes them in -- normally
the same value passed to \code{\link[=htc_gen_executable]{htc_gen_executable()}}. When \code{NULL} (the
default), resolves to \code{data_files} recorded in the manifest.}

\item{resources}{A named list with \code{cpus}, \code{memory}, and \code{disk}, or
\code{NULL}. When \code{NULL} (the default), resolves to the resolved resource
values \code{\link[=htc_gen_submit]{htc_gen_submit()}} recorded in the manifest (whichever preset,
or \code{custom_resources}, was actually used).}

\item{verbose}{Logical. If \code{TRUE} (the default), prints a line for every
check performed, not just the ones that found something. Set \code{FALSE}
to only see problems.}

\item{path}{A character string. Directory holding the job manifest
(\code{htc-manifest.yaml}). Defaults to \code{"."}, matching the default used
elsewhere in the family.}
}
\value{
A tibble with one row per issue found, columns \code{check},
\code{severity} (\code{"error"} or \code{"warning"}), and \code{message}. Zero rows means
nothing was found. Returned invisibly; call \code{print()} explicitly or
assign it to inspect the detail behind the printed summary.
}
\description{
\code{htc_check()} verifies, locally and in seconds, several things that would
otherwise only surface an hour later as a held or failed job on the
cluster (S-G4): that every file the job depends on actually exists on
this machine, that a \code{"multiple"}-mode job's subset files still match
what \code{subdatasets.csv} expects, that the resource request looks
plausible, and -- best effort, when a container tool is available
locally -- that the container image looks reachable.
}
\details{
Every argument can be resolved from the job manifest, so the common case
is \code{htc_check()} with no arguments at all, run right after
\code{\link[=htc_gen_submit]{htc_gen_submit()}} and \code{\link[=htc_gen_executable]{htc_gen_executable()}} and before \code{\link[=htc_upload]{htc_upload()}}.
}
\section{What counts as an error versus a warning}{

A missing input, data, or subset file is an \code{"error"}: the job will not
run without it, full stop. A resource request outside typical bounds, a
\code{container_image} tagged \code{latest}, or an image that could not be
confirmed pullable are \code{"warning"}s -- each might be exactly what you
intend, and none of them is something \code{htc_check()} can be certain about
from the local machine alone. \code{\link[=htc_upload]{htc_upload()}}'s own \code{check} argument only
blocks the upload on \code{"error"}s.
}

\section{The image check is best effort}{

\code{htc_check()} only attempts to confirm \code{container_image} is pullable when
\code{podman} or \code{docker} is found on the local \code{PATH}, via
\verb{<tool> manifest inspect}. A failure there is reported as a warning, not
an error, and is not conclusive either way: it may mean the image
genuinely does not exist, or simply that you are not logged in to the
registry from this machine, or that the tool timed out. When neither
tool is found, the image check is skipped entirely and reported as such.
}

\examples{
\donttest{
tmp <- withr::local_tempdir()
htc_gen_submit(
  container_image = "registry.doit.wisc.edu/netid/myimage:latest",
  resources       = "small",
  output          = tmp,
  path            = tmp
)
htc_check(path = tmp)
}
}
\seealso{
\code{\link[=htc_upload]{htc_upload()}}'s \code{check} argument, which runs this
automatically and aborts before uploading if an \code{"error"}-level issue
is found.
}
