% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-job-control.R
\name{htc_cancel}
\alias{htc_cancel}
\title{Cancel HTCondor jobs}
\usage{
htc_cancel(
  cluster_id = NULL,
  reason = NULL,
  config = NULL,
  dry_run = FALSE,
  verbose = FALSE,
  path = "."
)
}
\arguments{
\item{cluster_id}{An integer, character string, or \code{NULL}. The cluster ID
to remove, e.g. \code{6302860}. When \code{NULL} (the default), resolves to the
cluster ID recorded in the job manifest by the most recent
\code{\link[=htc_submit]{htc_submit()}} call. Unlike \code{\link[=htc_status]{htc_status()}}, \code{htc_cancel()} never falls
back to "all of my jobs": a bare \code{condor_rm} with no arguments removes
every job you have queued on the submit node, not just the ones from
this project, so the function errors instead of guessing when no
cluster ID is available anywhere.}

\item{reason}{A character string or \code{NULL}. An optional free-text reason
recorded in the job's HTCondor log (via \code{condor_rm}'s own \code{-reason}
flag), useful when revisiting a log later to remember why a cluster was
removed. Defaults to \code{NULL}.}

\item{config}{A named list as returned by \code{\link[=htc_config]{htc_config()}}. Must contain
\code{username} and \code{server}. If \code{NULL} (the default), uses the session
config set by \code{\link[=htc_start]{htc_start()}}. If no session config is set, the function
errors with instructions.}

\item{dry_run}{Logical. If \code{TRUE}, prints the SSH command that would be
executed without running it. Defaults to \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages. Defaults to
\code{FALSE}.}

\item{path}{A character string. Directory holding the job manifest
(\code{htc-manifest.yaml}), consulted only when \code{cluster_id} is \code{NULL}.
Defaults to \code{"."}, matching the default used elsewhere in the family.}
}
\value{
The \code{condor_rm} output as a character vector, returned invisibly.
}
\description{
\code{htc_cancel()} connects to an HTC submit node via SSH and runs
\code{condor_rm} to remove jobs from the queue -- a mistaken submission (the
wrong resource request, the wrong image, 500 jobs instead of 5) can be
stopped from R rather than requiring a manual SSH session (S-G2).
}
\section{Workflow}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{cfg <- htc_config()
job <- htc_submit(config = cfg)

# Realized the resource request was wrong -- stop it before it runs
htc_cancel(cluster_id = job, config = cfg, reason = "wrong request_memory")
}\if{html}{\out{</div>}}
}

\examples{
\donttest{
# Preview the SSH command without connecting to CHTC
cfg <- list(username = "netid", server = "ap2002.chtc.wisc.edu")
htc_cancel(cluster_id = 6302860, config = cfg, dry_run = TRUE)
}

\dontrun{
cfg <- htc_config()
htc_cancel(cluster_id = 6302860, config = cfg)
htc_cancel(cluster_id = 6302860, config = cfg, reason = "submitted by mistake")
}
}
\seealso{
\code{\link[=htc_release]{htc_release()}} to un-pause a held job instead of removing it,
and \code{\link[=htc_status]{htc_status()}} to see which jobs are running, idle, or held.
}
