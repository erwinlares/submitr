% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htc-helpers.R
\name{.shell_quote}
\alias{.shell_quote}
\title{Quote a string for a shell, but only when it needs quoting}
\usage{
.shell_quote(x)
}
\arguments{
\item{x}{A character string.}
}
\value{
A character string, quoted if necessary.
}
\description{
Internal helper. Quoting every argument is correct but makes \code{dry_run}
output noisy for the ordinary case: a reader checking the command before
running it should see \verb{condor_submit job.sub}, not
\verb{condor_submit 'job.sub'}. This quotes only strings containing a character
a shell would interpret, so the common case is byte-for-byte what submitr
produced before quoting existed, while an awkward name such as
\verb{Erwin's analysis.sub} still survives intact.
}
\keyword{internal}
